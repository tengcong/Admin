<p id="notice"><%= notice %></p>

<%= link_to 'Edit', edit_bq_package_path(@bq_package) %> |
<%= link_to 'Back', :back %>

<br>
<br>
<br>
<br>


<table class='table'>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td><%= @bq_package.tags %></td>
    </tr>

    <% @bqs.each do |bq| %>
      <tr>
        <td>
          <%= image_tag bq.try(:origin_url) %>

        </td>
        <td>
          <%= link_to '删除', bq, method: :delete, data: { confirm: 'Are you sure?' }, remote: true %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= paginate @bqs %>

