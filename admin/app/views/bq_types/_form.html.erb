<%= form_for(@bq_type) do |f| %>
  <% if @bq_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bq_type.errors.count, "error") %> prohibited this bq_type from being saved:</h2>

      <ul>
      <% @bq_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
